---
import ListSlide from "../../../../components/Slide/ListSlide.astro";
import SlidesLayout from "../../../../layouts/talks/embrace-the-patch.astro";

const nextHref = "/talks/embrace-the-patch/outro";
const nextLabel = "Go to the outro";
---

<SlidesLayout title="Slide 49.3: Location, Location, Location">
  <ListSlide
    title=" Location, Location, Location"
    next-href={nextHref}
    next-label={nextLabel}
    type="ol"
    sizer="Someone else wrote the fix: vendor ..."
  >
    <li>Someone else wrote the fix: <code>/vendor</code></li>
    <li>"You" wrote the fix: <code>/lib</code></li>
    <li>Tip: Add all requires to <code>/lib/patches.rb</code></li>
  </ListSlide>
</SlidesLayout>
