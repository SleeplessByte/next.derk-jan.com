---
import ListSlide from "../../../../components/Slide/ListSlide.astro";
import SlidesLayout from "../../../../layouts/talks/embrace-the-patch.astro";

const nextHref = "/talks/embrace-the-patch/43";
const nextLabel = "Go to slide 43: Monkeypatch";
---

<SlidesLayout title="Slide 42.4: Patching without patch files">
  <ListSlide
    title="Patching without patch files"
    class="--red"
    next-href={nextHref}
    next-label={nextLabel}
    type="ul"
    sizer="Forking (when using bundler + git)"
  >
    <li>Monkeypatch 🐒</li>
    <li>Refinement (Monkeypatch 2.0?)</li>
    <li>JIT patch (ie. using Rails hooks)</li>
    <li>Vendoring</li>
    <li>Forking <em>(when using bundler + git)</em></li>
  </ListSlide>
</SlidesLayout>
