---
import { Code } from "astro:components";
import CodeSlide from "../../../../components/Slide/CodeSlide.astro";
import EmbeddedCodeSlide from "../../../../components/Slide/EmbeddedCodeSlide.astro";
import SlidesLayout from "../../../../layouts/talks/embrace-the-patch.astro";

const nextHref = "/talks/embrace-the-patch/09/5-diff";
const nextLabel = "Go to slide 9.5: explaining diff";
---

<SlidesLayout title="Slide 9.4: explaining diff">
  <EmbeddedCodeSlide next-href={nextHref} next-label={nextLabel}>
    <CodeSlide slide:embedded slot="embedded">
      <Code
        slot="before"
        lang="ruby"
        code={`
class HelloWorld
  def self.hello
    "Goodbye, Mars!"
  end
end
`.trim()}
      />

      <Code
        slot="after"
        lang="ruby"
        code={`
# frozen_string_literal: true

class HelloWorld
  def self.hello
    "Hello, World!"
  end
end
`.trim()}
      />
    </CodeSlide>

    <Code slot="before" lang="shell" code={`$`} />

    <Code slot="after" lang="diff" code={``.trim()} />
  </EmbeddedCodeSlide>
</SlidesLayout>
