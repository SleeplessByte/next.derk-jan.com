---
import { AccessibleCardLink } from "./Component.tsx";

const { className } = Astro.props;
---

<AccessibleCardLink.Extension client:idle className={className}>
  <slot />
</AccessibleCardLink.Extension>
